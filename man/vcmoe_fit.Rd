% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{vcmoe_fit}
\alias{vcmoe_fit}
\title{Fit a varying-coefficient mixture-of-experts model}
\usage{
vcmoe_fit(
  formula,
  data,
  u,
  k = 2L,
  family = "gaussian",
  bandwidth = NULL,
  u_grid = NULL,
  control = list(),
  label = "align",
  parameterization = "a1_epanechnikov_scaled",
  u_scale = c("unit", "none"),
  engine = c("local_grid_em", "joint_path_em"),
  progress = NULL
)
}
\arguments{
\item{formula}{A formula of the form \code{y ~ expert_terms | gating_terms}.
For grouped binomial data, use \code{cbind(success, failure) ~ expert_terms | gating_terms}. For Negative-Binomial count data, expert-side
\code{offset(log_size_factor)} terms are supported.}

\item{data}{A data frame.}

\item{u}{Continuous index column name or numeric vector.}

\item{k}{Number of mixture components. \code{k = 2} is the primary v0 target;
\code{k = 3:10} are high-k candidate support and require diagnostics before
being treated as stable.}

\item{family}{Model family. \code{"gaussian"}, \code{"binomial"}, and
\code{"negative-binomial"} are implemented.}

\item{bandwidth}{Kernel bandwidth. If \code{NULL}, a Silverman-style default is
used for \code{u}.}

\item{u_grid}{Grid where coefficient functions are estimated.}

\item{control}{Named list overriding EM and label-alignment settings.}

\item{label}{Label strategy. \code{"align"} uses exact global alignment for
\code{k <= 6} and sequential assignment with ambiguity margins for \code{k >= 7};
\code{"global"} requests exact global alignment when feasible and falls back
to the same sequential assignment path for \code{k >= 7};
\code{"greedy"} keeps the older one-step alignment.}

\item{parameterization}{Estimator convention. The public package uses
\code{"a1_epanechnikov_scaled"}: Epanechnikov density weights \code{K((u-u0)/h)/h}
and the scaled local-linear basis \code{(u-u0)/h}.}

\item{u_scale}{How to transform \code{u} before fitting. The default \code{"unit"}
maps complete-row \code{u} values to \verb{[0, 1]}; \code{"none"} leaves \code{u} on the
supplied scale. \code{bandwidth} and \code{u_grid} are interpreted on the transformed
analysis scale.}

\item{engine}{Fitting engine. The default \code{"local_grid_em"} preserves the
original independent local-grid EM path. \code{"joint_path_em"} updates a
shared observation-level responsibility path across grid points.}

\item{progress}{Joint-path progress reporting. The default \code{NULL} is silent;
use \code{TRUE} for messages or a single CSV file path for structured logging.
Iteration frequency is controlled by \code{control$progress_every}.}
}
\value{
An object of class \code{vcmoe}.
}
\description{
Fit a varying-coefficient mixture-of-experts model
}
\details{
Rows with missing or non-finite response, covariates, or \code{u} are
removed consistently before fitting, with a warning. For single-trial
Bernoulli responses, the default gating ridge is strengthened to
\code{control$ridge = 1} unless the user explicitly supplies \code{control$ridge};
grouped Binomial and other families keep the global default. Joint-path
traces record the sample-level nearest-grid log-likelihood as a diagnostic
criterion. The label-consistent updates do not guarantee that this
diagnostic is monotone; convergence is based on posterior and parameter
deltas instead.
}

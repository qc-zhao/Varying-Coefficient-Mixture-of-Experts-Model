% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bandwidth.R
\name{vcmoe_select_bandwidth}
\alias{vcmoe_select_bandwidth}
\title{Select a VCMoE bandwidth by K-fold cross-validation}
\usage{
vcmoe_select_bandwidth(
  formula,
  data,
  u,
  k = 2L,
  family = "gaussian",
  bandwidth_grid = NULL,
  folds = 5L,
  u_grid = NULL,
  control = list(),
  label = "align",
  parameterization = "a1_epanechnikov_scaled",
  u_scale = c("unit", "none"),
  seed = NULL,
  refit = TRUE,
  engine = c("local_grid_em", "joint_path_em")
)
}
\arguments{
\item{formula}{A formula of the form \code{y ~ expert_terms | gating_terms}.}

\item{data}{A data frame.}

\item{u}{Continuous index column name or numeric vector.}

\item{k}{Number of mixture components. Values from 2 through 10 are
supported.}

\item{family}{Model family. \code{"gaussian"}, \code{"binomial"}, and
\code{"negative-binomial"} are supported.}

\item{bandwidth_grid}{Candidate bandwidth values. If \code{NULL}, uses multiples
of the default bandwidth.}

\item{folds}{Number of random cross-validation folds.}

\item{u_grid}{Grid where coefficient functions are estimated.}

\item{control}{Named list passed to the selected fitting engine.}

\item{label}{Label strategy passed to the selected fitting engine.}

\item{parameterization}{Estimator convention passed to the selected fitting
engine.}

\item{u_scale}{\code{u} scaling strategy passed to the selected fitting engine.}

\item{seed}{Optional random seed for fold assignment and, when
\code{control$seed} is absent, deterministic CV refits.}

\item{refit}{Whether to refit the final model on all data using the selected
bandwidth.}

\item{engine}{Fitting engine used for every cross-validation fold and the
optional final refit. The default \code{"local_grid_em"} preserves the 0.1.0
behavior; \code{"joint_path_em"} uses the joint-path engine of \code{vcmoe_fit()}.}
}
\value{
An object of class \code{vcmoe_bandwidth_selection}.
}
\description{
Select a VCMoE bandwidth by K-fold cross-validation
}
